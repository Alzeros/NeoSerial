Rust 串口调试工具 UI 设计需求（参考传统工程工具风格）
项目目标

我要使用 Rust 重写一个传统串口调试工具（类似 SSCOM、XCOM、AT 调试工具），UI 风格希望保持经典工程软件的设计，而不是现代化的 Material Design 或 Fluent Design。

整体原则：

信息密度高
所有功能尽量一屏完成
面向研发/测试工程师
优先效率而不是美观
可以增加新功能，但不要改变整体交互逻辑

整个界面遵循"顶部配置 + 中间工作区 + 底部操作区"的布局。

一、整体布局

整个窗口采用三层布局。

┌──────────────────────────────────────────────┐
│ 顶部：串口连接配置                            │
├───────────────────────┬──────────────────────┤
│                       │                      │
│ 左：串口日志窗口       │ 右：命令脚本列表      │
│                       │                      │
│                       │                      │
├───────────────────────┴──────────────────────┤
│ 底部：发送命令、记录、状态                    │
└──────────────────────────────────────────────┘

布局要求：

顶部固定高度。

中间左右分栏。

左侧约占70%。

右侧约占30%。

底部固定高度。

窗口缩放时，中间区域自适应。

二、顶部——串口配置栏（Connection Toolbar）

顶部只放连接相关配置。

包含：

COM选择
波特率
数据位
校验位
停止位
流控
Connect / Disconnect 按钮

布局要求：

全部横向排列。

每个控件高度一致。

标签放在控件左侧。

整个区域尽量紧凑。

类似：

COM     [COM5 ▼]

Baud    [115200 ▼]

Data    [8 ▼]

Parity  [None ▼]

Stop    [1 ▼]

Flow    [None ▼]

[ Connect ]

连接按钮使用醒目的主色。

三、中间左侧——串口日志区（Serial Console）

这是整个软件最重要的区域。

需要占据最大空间。

要求：

白色背景。

支持大量文本。

自动滚动。

可复制。

支持清空。

支持搜索。

以后可以增加：

时间戳
收发颜色区分
日志过滤
正则搜索
保存日志

日志区域不要做卡片。

不要阴影。

不要圆角。

就是一个大型文本区域。

四、中间右侧——命令脚本区（Script Panel）

这是整个工具最大的特色。

不是按钮列表。

而是一组"命令队列"。

顶部采用 Tab。

例如：

HTTP

MQTT

TCP

SMS

GNSS

每个 Tab 保存一组命令。

每条命令包含：

□ 是否启用

命令内容

HEX发送

发送Enter

执行顺序

发送延迟(ms)

整个区域实际上应该设计成：

CommandGroup

└── CommandItem[]

每一行都是一个 CommandItem。

以后方便扩展：

超时
重试次数
等待指定回复
条件执行
循环
变量替换
Lua脚本
Python脚本

因此UI不要写死。

应基于数据模型生成。

五、底部——发送区域（Quick Send）

底部用于临时发送命令。

包含：

输入框：

AT+CFUN=1

发送按钮。

以及：

HEX发送

HEX显示

自动换行

发送回车

发送文件

记录日志

等等。

这里属于快速发送。

不要与右侧脚本区混合。

因为：

右侧负责固定脚本。

底部负责临时命令。

这是两个完全不同的使用场景。

六、状态栏（建议新增）

建议底部增加状态栏。

例如：

Connected

COM5

115200

TX: 10234 Bytes

RX: 83912 Bytes

Running

Script: HTTP

所有状态实时刷新。

七、按钮设计

颜色不要过多。

建议：

蓝色：

主要操作

例如：

Connect

Run

Send

Save

Load

绿色：

成功状态。

红色：

Stop

Disconnect

Delete

灰色：

普通功能。

整个软件颜色控制在3~4种以内。

八、整体视觉风格

不是现代App。

而是经典工程软件。

参考：

SSCOM
XCOM
SecureCRT
PuTTY
Wireshark
QXDM
Keil
IAR

特点：

信息密度高。

控件之间间距小。

没有大量留白。

没有圆角卡片。

没有复杂动画。

所有功能入口尽量保持一级可达。

九、Rust 实现建议

UI 与业务解耦。

建议抽象数据模型：

ConnectionConfig

SerialSession

LogEntry

CommandItem

CommandGroup

ScriptWorkspace

QuickSend

Settings

UI 仅负责展示这些对象。

不要把业务逻辑直接写进界面。

十、未来扩展能力

整个UI需要支持以后增加：

多串口同时打开
多标签Console
多设备管理
自动测试流程
Lua脚本
Python脚本
AT脚本
MQTT调试
TCP调试
HTTP调试
FTP调试
日志过滤
搜索
高亮
收藏命令
历史命令
快捷键
深色主题

因此整个UI要模块化。

每个区域都是独立组件。

布局保持稳定。

增加功能时不要影响已有交互。

核心设计理念（最重要）

这不是一个追求视觉效果的软件，而是一个工程研发工具。

设计目标按优先级排序：

操作效率
信息密度
一屏完成所有常用操作
状态反馈清晰
学习成本低
长时间使用不疲劳
可扩展性强
最后才考虑美观

**一句话总结：**保留经典串口调试工具的高效工程风格，采用现代 Rust 架构实现，在不改变核心交互模式的前提下，提高可维护性、可扩展性和用户体验。