# NeoSerial UI 重构实现计划（经典工程软件风格）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 NeoSerial 从单栏紧凑工具重构为经典工程软件风格（SSCOM/XCOM）：三层布局（顶部配置/中间左右分栏/底部发送+状态栏）、右侧命令脚本侧边栏框架、格式化文本存盘、时间戳与记录发送开关、紧凑互斥行尾控件、日志区白底。

**Architecture:** 存盘改为格式化文本日志（`[ts] [tag] content`），由读线程在切出完整行后按显示开关生成行文本直送 FileLogger（独立于 UI Timer，暂停照写，只存完整行）。UI 拆为多个独立 Slint 组件（顶部配置栏、工具栏、日志区、侧边栏、发送区、状态栏），App 层接线与持久化。Settings 扩展开关字段与 CommandGroup 模型。

**Tech Stack:** Rust + Slint 1.17 + serialport 4.9 + crossbeam-channel 0.5 + rfd 0.14 + serde/serde_json。

## Global Constraints

- **Rust 工具链**：cargo 在 `S:\rust\.cargo\bin\`，bash 命令前必须 `export PATH="/s/rust/.cargo/bin:$PATH"`。
- **镜像**：项目级 `.cargo/config.toml` 已覆盖为 `sparse+https://rsproxy.cn/index/`，勿改全局配置。
- **测试策略**：功能调试阶段，每个 Task 只跑涉及变更点的测试，不全量跑（用户要求）。
- **connection/ 模块禁止依赖 slint**（架构约束，不变）。
- **时间戳**：手写 + 硬编码 UTC+8，不用 chrono。已有 `util::time_fmt::{now_local_ts, now_local_compact}`。
- **方向 tag**：中文 `[发送]` / `[接收]`，固定字符串。
- **存盘格式**：格式化文本日志，默认后缀 `.log`。半行（未收到换行符）不落盘，等行结束才写。
- **配色**：日志区白底黑字（`#ffffff` 背景），其余面板深色（`#262b33`）。TX 深绿、RX 深灰/黑、ERROR 红，须在白底可辨。
- **YAGNI**：CommandItem 本轮只 label/content/mode/ending/enabled 五字段，不加执行顺序/延迟等（顺序执行引擎留后续）。
- **git 提交作者**：`Alzeros <82042404+Alzeros@users.noreply.github.com>`（已配 local config）。
- **Slint 1.17 API**：`Timer::default().start()`；`ModelRc::new(...)`（非 From trait）；条件渲染 `if cond : ElementName { }`（带类型名和冒号，一个 if 一个元素）；`Window size()/position()` 返回 `PhysicalSize<u32>`/`PhysicalPosition<i32>`。

---

## 文件结构总览

### 新建
- `src/util/log_format.rs` — 行格式化纯函数：根据开关生成 `[ts] [tag] content` 字符串。
- `src/config/command_group.rs` — CommandGroup/CommandItem 数据模型 + 序列化。
- `ui/components/status-bar.slint` — 底部状态栏组件。
- `ui/components/script-panel.slint` — 右侧命令脚本侧边栏组件。
- `ui/components/seg-button.slint` — 紧凑互斥按钮组组件（行尾选择等复用）。

### 修改
- `src/config/settings.rs` — 加 `show_timestamp`/`log_send` 字段；`quick_commands` 迁移为 `command_groups`。
- `src/config/mod.rs` — 声明 `command_group` 模块。
- `src/connection/handle.rs` — `SetLogger` 改为 `Option<Sender<String>>`（文本行）；新增 `SetDisplayConfig(DisplayConfig)`；`ConnEvent` 新增 `Stats { tx_bytes, rx_bytes }`。
- `src/connection/reader.rs` — 存盘改送格式化行文本；按 DisplayConfig 过滤；上报字节统计。
- `src/connection/mod.rs` — `spawn_connection` 参数调整（log_tx 类型 + DisplayConfig）。
- `src/logging/file_logger.rs` — 改写 `String` 行文本；`current_path()` 已有。
- `src/buffer/log_line.rs` — 不变（已有 dir/ts 足够）。
- `src/app.rs` — 接线新 UI、开关、串口参数、状态栏、侧边栏增删改、持久化。
- `ui/app.slint` — 三层布局重写 + 新属性/callback。
- `ui/components/connection-bar.slint` — 加串口参数控件 + 标签左置。
- `ui/components/toolbar.slint` — 加时间戳/记录发送开关。
- `ui/components/log-view.slint` — 白底黑字 + 行格式（ts/tag/content 分列）。
- `ui/components/send-bar.slint` — 行尾改互斥按钮组 + HEX 开关。
- `ui/styles/theme.slint` — 加白底日志配色常量。

---

## Task 1: 行格式化纯函数（util/log_format.rs）

**Files:**
- Create: `src/util/log_format.rs`
- Modify: `src/util/mod.rs`（声明模块）
- Test: `src/util/log_format.rs` 内 `#[cfg(test)]`

**Interfaces:**
- Consumes: `crate::buffer::log_line::{LogLine, Dir}`
- Produces:
  - `pub struct DisplayConfig { pub show_timestamp: bool, pub log_send: bool }`
  - `pub fn format_line(line: &LogLine, cfg: &DisplayConfig, is_hex: bool) -> String` — 返回 `[ts] [tag] content` 或省略形式的单行字符串（不含末尾换行符）

**说明**：这是纯函数，根据开关把 LogLine 格式化成屏幕/存盘共用的行文本。tag 用中文 `[发送]`/`[接收]`。`log_send=false` 且 `dir==Tx` 时返回空字符串（表示该行不记录）。`is_hex` 控制内容用 hex dump 还是 ascii。

- [ ] **Step 1: 写失败测试**

在 `src/util/log_format.rs`：

```rust
use crate::buffer::log_line::{Dir, LogLine};

/// 显示与存盘共用的开关。
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct DisplayConfig {
    pub show_timestamp: bool,
    pub log_send: bool,
}

/// 把一行 LogLine 格式化为 "[ts] [tag] content" 形式的字符串（不含末尾换行符）。
/// - show_timestamp=false 时省略时间戳前缀。
/// - log_send=false 且方向为 Tx 时返回空字符串（该行不记录）。
/// - is_hex=true 时 content 用 hex dump，否则用 ascii。
pub fn format_line(line: &LogLine, cfg: &DisplayConfig, is_hex: bool) -> String {
    if !cfg.log_send && matches!(line.dir, Dir::Tx) {
        return String::new();
    }
    let tag = if matches!(line.dir, Dir::Tx) { "[发送]" } else { "[接收]" };
    let content = if is_hex { &line.hex } else { &line.ascii };
    if cfg.show_timestamp {
        format!("{} {} {}", line.ts, tag, content)
    } else {
        format!("{} {}", tag, content)
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn rx_line(ts: &str, content: &[u8]) -> LogLine {
        LogLine::new(ts.into(), Dir::Rx, content.to_vec(), &[])
    }
    fn tx_line(ts: &str, content: &[u8]) -> LogLine {
        LogLine::new(ts.into(), Dir::Tx, content.to_vec(), &[])
    }

    #[test]
    fn test_rx_with_timestamp() {
        let l = rx_line("08:00:01.123", b"OK");
        let cfg = DisplayConfig { show_timestamp: true, log_send: true };
        assert_eq!(format_line(&l, &cfg, false), "08:00:01.123 [接收] OK");
    }

    #[test]
    fn test_tx_with_timestamp() {
        let l = tx_line("08:00:01.000", b"AT");
        let cfg = DisplayConfig { show_timestamp: true, log_send: true };
        assert_eq!(format_line(&l, &cfg, false), "08:00:01.000 [发送] AT");
    }

    #[test]
    fn test_no_timestamp() {
        let l = rx_line("08:00:01.123", b"OK");
        let cfg = DisplayConfig { show_timestamp: false, log_send: true };
        assert_eq!(format_line(&l, &cfg, false), "[接收] OK");
    }

    #[test]
    fn test_log_send_false_drops_tx() {
        let l = tx_line("08:00:01.000", b"AT");
        let cfg = DisplayConfig { show_timestamp: true, log_send: false };
        assert_eq!(format_line(&l, &cfg, false), "");
    }

    #[test]
    fn test_log_send_false_keeps_rx() {
        let l = rx_line("08:00:01.123", b"OK");
        let cfg = DisplayConfig { show_timestamp: true, log_send: false };
        assert_eq!(format_line(&l, &cfg, false), "08:00:01.123 [接收] OK");
    }

    #[test]
    fn test_hex_mode() {
        let l = rx_line("08:00:01.123", b"AT");
        let cfg = DisplayConfig { show_timestamp: false, log_send: true };
        let s = format_line(&l, &cfg, true);
        assert!(s.starts_with("[接收] "));
        assert!(s.contains("41 54"));
    }
}
```

- [ ] **Step 2: 声明模块**

在 `src/util/mod.rs` 加一行 `pub mod log_format;`（先看该文件现有内容，按字母序插入）。

- [ ] **Step 3: 运行测试验证通过**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo test --bin neoserial util::log_format`
Expected: 6 passed; 0 failed

- [ ] **Step 4: 提交**

```bash
git add src/util/log_format.rs src/util/mod.rs
git commit -m "feat: 行格式化纯函数 format_line + DisplayConfig"
```

---

## Task 2: CommandGroup/CommandItem 数据模型

**Files:**
- Create: `src/config/command_group.rs`
- Modify: `src/config/mod.rs`（声明模块）
- Test: `src/config/command_group.rs` 内 `#[cfg(test)]`

**Interfaces:**
- Consumes: `crate::config::settings::{DisplayMode, LineEnding}`（已有枚举）
- Produces:
  - `pub struct CommandItem { pub label: String, pub content: String, pub mode: DisplayMode, pub line_ending: LineEnding, pub enabled: bool }`
  - `pub struct CommandGroup { pub name: String, pub items: Vec<CommandItem> }`
  - `impl CommandGroup { pub fn default_group() -> Self }` — 含 AT/CSQ/Reset/HEX5A 四条示例命令

- [ ] **Step 1: 写失败测试**

`src/config/command_group.rs`：

```rust
use crate::config::settings::{DisplayMode, LineEnding};
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, Serialize, Deserialize)]
pub struct CommandItem {
    pub label: String,
    pub content: String,
    pub mode: DisplayMode,
    pub line_ending: LineEnding,
    pub enabled: bool,
}

#[derive(Clone, Debug, Serialize, Deserialize)]
pub struct CommandGroup {
    pub name: String,
    pub items: Vec<CommandItem>,
}

impl CommandGroup {
    /// 默认命令组：含 AT / CSQ / Reset / HEX5A 四条示例。
    pub fn default_group() -> Self {
        CommandGroup {
            name: "默认".into(),
            items: vec![
                CommandItem {
                    label: "AT".into(), content: "AT".into(),
                    mode: DisplayMode::Ascii, line_ending: LineEnding::Crlf, enabled: true,
                },
                CommandItem {
                    label: "CSQ".into(), content: "AT+CSQ".into(),
                    mode: DisplayMode::Ascii, line_ending: LineEnding::Crlf, enabled: true,
                },
                CommandItem {
                    label: "Reset".into(), content: "AT+CFUN=1,1".into(),
                    mode: DisplayMode::Ascii, line_ending: LineEnding::Crlf, enabled: true,
                },
                CommandItem {
                    label: "HEX5A".into(), content: "5A A5 01 02".into(),
                    mode: DisplayMode::Hex, line_ending: LineEnding::None, enabled: true,
                },
            ],
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_default_group_has_commands() {
        let g = CommandGroup::default_group();
        assert_eq!(g.name, "默认");
        assert_eq!(g.items.len(), 4);
        assert_eq!(g.items[0].label, "AT");
    }

    #[test]
    fn test_serde_roundtrip() {
        let g = CommandGroup::default_group();
        let json = serde_json::to_string(&g).unwrap();
        let back: CommandGroup = serde_json::from_str(&json).unwrap();
        assert_eq!(back.name, g.name);
        assert_eq!(back.items.len(), g.items.len());
        assert_eq!(back.items[3].label, "HEX5A");
        assert!(back.items[3].enabled);
    }
}
```

- [ ] **Step 2: 声明模块**

`src/config/mod.rs` 加 `pub mod command_group;`。

- [ ] **Step 3: 运行测试**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo test --bin neoserial config::command_group`
Expected: 2 passed; 0 failed

- [ ] **Step 4: 提交**

```bash
git add src/config/command_group.rs src/config/mod.rs
git commit -m "feat: CommandGroup/CommandItem 数据模型"
```

---

## Task 3: Settings 扩展（开关字段 + command_groups 迁移）

**Files:**
- Modify: `src/config/settings.rs`
- Test: `src/config/settings.rs` 内 `#[cfg(test)]`

**Interfaces:**
- Consumes: `crate::config::command_group::CommandGroup`（Task 2）
- Produces:
  - `UiSettings` 加 `pub show_timestamp: bool`、`pub log_send: bool`（默认 true / true）
  - `Settings` 字段 `quick_commands` 改为 `command_groups: Vec<CommandGroup>`（默认含一个 default_group）
  - `Settings::default_settings()` 更新
  - 向后兼容迁移在 `load_from` 中处理：若反序列化失败，尝试旧格式（`quick_commands`）迁移

**说明**：直接改字段名会破坏旧 settings.json 的反序列化。迁移策略：`load_from` 反序列化到新 `Settings` 失败时，尝试反序列化到一个临时 `LegacySettings`（含 `quick_commands: Vec<QuickCommand>`），成功则迁移为 `command_groups`，失败才走原 corrupt 路径（备份 .bad + default）。

- [ ] **Step 1: 写失败测试**（追加到 settings.rs 的 tests 模块）

```rust
    #[test]
    fn test_default_has_display_switches() {
        let s = Settings::default_settings();
        assert!(s.ui.show_timestamp);
        assert!(s.ui.log_send);
    }

    #[test]
    fn test_default_has_command_groups() {
        let s = Settings::default_settings();
        assert_eq!(s.command_groups.len(), 1);
        assert_eq!(s.command_groups[0].name, "默认");
        assert!(!s.command_groups[0].items.is_empty());
    }

    #[test]
    fn test_migrate_legacy_quick_commands() {
        let dir = std::env::temp_dir().join(format!(
            "neoserial_test_{}_legacy", std::process::id()
        ));
        let _ = std::fs::remove_dir_all(&dir);
        std::fs::create_dir_all(&dir).unwrap();
        let path = dir.join("settings.json");
        // 旧格式：含 quick_commands，无 command_groups
        let legacy = r#"{
            "version": 1,
            "window": {"width": 1100, "height": 720, "x": 100, "y": 80},
            "serial_defaults": {"baud_rate": 115200, "data_bits": "Eight", "parity": "None", "stop_bits": "One", "flow_control": "None"},
            "last_port": "",
            "ui": {"display_mode": "Ascii", "line_ending": "Crlf", "auto_scroll": true, "ring_buffer_capacity": 5000},
            "quick_commands": [{"label": "AT", "content": "AT", "mode": "Ascii", "line_ending": "Crlf"}],
            "error_keywords": ["ERROR"]
        }"#;
        std::fs::write(&path, legacy).unwrap();

        let loaded = Settings::load_from(&path);
        // 旧 quick_commands 应迁移为 command_groups
        assert_eq!(loaded.command_groups.len(), 1);
        assert_eq!(loaded.command_groups[0].items.len(), 1);
        assert_eq!(loaded.command_groups[0].items[0].label, "AT");
        // 开关字段缺失应取默认
        assert!(loaded.ui.show_timestamp);
        assert!(loaded.ui.log_send);

        let _ = std::fs::remove_dir_all(&dir);
    }
```

- [ ] **Step 2: 运行测试验证失败**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo test --bin neoserial config::settings`
Expected: 新增 3 个测试编译失败/失败（字段不存在）

- [ ] **Step 3: 修改 settings.rs 实现**

(a) 在文件顶部 use 区加：
```rust
use crate::config::command_group::CommandGroup;
```

(b) `UiSettings` 加两字段：
```rust
pub struct UiSettings {
    pub display_mode: DisplayMode,
    pub line_ending: LineEnding,
    pub auto_scroll: bool,
    pub ring_buffer_capacity: usize,
    pub show_timestamp: bool,
    pub log_send: bool,
}
```

(c) `Settings` 把 `quick_commands` 字段改为 `command_groups`：
```rust
pub struct Settings {
    pub version: u32,
    pub window: WindowSettings,
    pub serial_defaults: SerialDefaults,
    pub last_port: String,
    pub ui: UiSettings,
    pub command_groups: Vec<CommandGroup>,
    pub error_keywords: Vec<String>,
}
```

(d) `default_settings()` 中：
- `ui` 加 `show_timestamp: true, log_send: true,`
- 删除原 `quick_commands: vec![...]`，改为 `command_groups: vec![CommandGroup::default_group()],`
- 删除原 `QuickCommand` 相关导入使用（若 `QuickCommand` struct 不再被用，保留 struct 定义无害，但 default 不再构造它）

(e) `load_from` 加旧格式迁移。替换现有 `load_from` 函数体为：

```rust
    pub(crate) fn load_from(path: &Path) -> Self {
        let text = match fs::read_to_string(path) {
            Ok(t) => t,
            Err(_) => return Self::default_settings(),
        };
        // 先尝试新格式
        if let Ok(s) = serde_json::from_str::<Settings>(&text) {
            return s;
        }
        // 再尝试旧格式迁移（quick_commands → command_groups）
        if let Ok(legacy) = serde_json::from_str::<LegacySettings>(&text) {
            return legacy.migrate();
        }
        // 都失败：备份 + 默认
        let _ = fs::rename(path, path.with_extension("json.bad"));
        Self::default_settings()
    }
```

(f) 在 `impl Settings { ... }` 块之后、tests 之前，加 LegacySettings：

```rust
/// 旧版配置（quick_commands）。用于向后兼容迁移。
#[derive(Deserialize)]
struct LegacySettings {
    version: u32,
    window: WindowSettings,
    serial_defaults: SerialDefaults,
    last_port: String,
    ui: LegacyUi,
    quick_commands: Vec<QuickCommand>,
    error_keywords: Vec<String>,
}

#[derive(Deserialize)]
struct LegacyUi {
    display_mode: DisplayMode,
    line_ending: LineEnding,
    auto_scroll: bool,
    ring_buffer_capacity: usize,
}

impl LegacySettings {
    fn migrate(self) -> Settings {
        let def = Settings::default_settings();
        Settings {
            version: self.version,
            window: self.window,
            serial_defaults: self.serial_defaults,
            last_port: self.last_port,
            ui: UiSettings {
                display_mode: self.ui.display_mode,
                line_ending: self.ui.line_ending,
                auto_scroll: self.ui.auto_scroll,
                ring_buffer_capacity: self.ui.ring_buffer_capacity,
                show_timestamp: def.ui.show_timestamp,
                log_send: def.ui.log_send,
            },
            command_groups: vec![CommandGroup {
                name: "默认".into(),
                items: self.quick_commands.into_iter().map(|q| crate::config::command_group::CommandItem {
                    label: q.label,
                    content: q.content,
                    mode: q.mode,
                    line_ending: q.line_ending,
                    enabled: true,
                }).collect(),
            }],
            error_keywords: self.error_keywords,
        }
    }
}
```

- [ ] **Step 4: 修复现有测试引用 `quick_commands` 的地方**

settings.rs 现有测试 `test_default_quick_commands` 断言 `s.quick_commands`，改为断言 `s.command_groups`：
```rust
    #[test]
    fn test_default_quick_commands() {
        let s = Settings::default_settings();
        assert!(!s.command_groups.is_empty());
        assert_eq!(s.command_groups[0].items[0].label, "AT");
    }
```
`test_serde_roundtrip` 中 `back.quick_commands.len()` 改为 `back.command_groups.len()`。

- [ ] **Step 5: 运行测试验证通过**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo test --bin neoserial config::settings`
Expected: 全部 passed（含新增 3 个）

- [ ] **Step 6: 提交**

```bash
git add src/config/settings.rs
git commit -m "feat: Settings 加 show_timestamp/log_send + command_groups 迁移"
```

---

## Task 4: FileLogger 改为写文本行

**Files:**
- Modify: `src/logging/file_logger.rs`
- Test: `src/logging/file_logger.rs` 内 `#[cfg(test)]`

**Interfaces:**
- Consumes: 无新依赖
- Produces:
  - `FileLogger` 的 channel 类型从 `Vec<u8>` 改为 `String`（每条是一行文本，**不含**末尾换行符，FileLogger 写入时补 `\n`）
  - `pub fn start(path: PathBuf, error_tx: Sender<String>) -> io::Result<Self>`（签名不变）
  - `pub fn log_line(&self, line: String)`（替代旧 log_sender 的用法，send 一行文本）
  - `pub fn line_sender(&self) -> Sender<String>`（供读线程持有 clone）
  - `pub fn current_path(&self) -> Option<String>`（不变）
  - `pub fn stop(self)`（不变）

**说明**：FileLogger 收到每条 String 后 `write_all(line) + write_all(b"\n")`。UTF-8 文本，等宽可读。

- [ ] **Step 1: 改写 file_logger.rs**

```rust
use crossbeam_channel::{Sender, unbounded};
use std::fs::File;
use std::io::{BufWriter, Write};
use std::path::PathBuf;
use std::sync::Mutex;

/// 存盘句柄。每条记录是一行文本（不含末尾换行符），存盘线程写入时补 \n。
/// channel send 纳秒级，绝不阻塞调用方（读线程）。
pub struct FileLogger {
    tx: Sender<String>,
    path: Mutex<Option<PathBuf>>,
}

impl FileLogger {
    pub fn start(path: PathBuf, error_tx: Sender<String>) -> std::io::Result<Self> {
        let file = File::create(&path)?;
        let (tx, rx) = unbounded::<String>();

        std::thread::Builder::new()
            .name("neoserial-file-logger".into())
            .spawn(move || {
                let mut writer = BufWriter::new(file);
                let mut error_msg: Option<String> = None;
                for line in rx.iter() {
                    if let Err(e) = writer.write_all(line.as_bytes()) {
                        error_msg = Some(format!("存盘失败: {}", e));
                        break;
                    }
                    if let Err(e) = writer.write_all(b"\n") {
                        error_msg = Some(format!("存盘失败: {}", e));
                        break;
                    }
                    if let Err(e) = writer.flush() {
                        error_msg = Some(format!("存盘失败: {}", e));
                        break;
                    }
                }
                let _ = writer.flush();
                if let Some(msg) = error_msg {
                    let _ = error_tx.send(msg);
                }
            })?;

        Ok(FileLogger {
            tx,
            path: Mutex::new(Some(path)),
        })
    }

    /// 返回 tx 的 clone，供读线程直接 send 行文本。
    pub fn line_sender(&self) -> Sender<String> {
        self.tx.clone()
    }

    /// 记录一行文本（非阻塞）。传入的字符串不应含末尾换行符。
    pub fn log_line(&self, line: String) {
        let _ = self.tx.send(line);
    }

    /// 当前存盘文件路径。stop 之后返回 None。
    pub fn current_path(&self) -> Option<String> {
        self.path
            .lock()
            .ok()
            .and_then(|guard| guard.as_ref().map(|p| p.to_string_lossy().to_string()))
    }

    pub fn stop(self) {
        drop(self.tx);
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::io::Read;

    #[test]
    fn test_file_logger_writes_lines() {
        let dir = std::env::temp_dir();
        let path = dir.join(format!("neoserial_test_{}.log", std::process::id()));
        let (error_tx, error_rx) = crossbeam_channel::unbounded::<String>();
        let logger = FileLogger::start(path.clone(), error_tx).unwrap();
        let tx = logger.line_sender();
        let _ = tx.send("08:00:01.000 [发送] AT".to_string());
        let _ = tx.send("08:00:01.123 [接收] OK".to_string());
        logger.stop();

        std::thread::sleep(std::time::Duration::from_millis(100));
        let mut f = File::open(&path).unwrap();
        let mut s = String::new();
        f.read_to_string(&mut s).unwrap();
        assert_eq!(s, "08:00:01.000 [发送] AT\n08:00:01.123 [接收] OK\n");
        assert!(error_rx.try_recv().is_err());
        let _ = std::fs::remove_file(&path);
    }

    #[test]
    fn test_current_path() {
        let dir = std::env::temp_dir();
        let path = dir.join(format!("neoserial_test_path_{}.log", std::process::id()));
        let (error_tx, _) = crossbeam_channel::unbounded::<String>();
        let logger = FileLogger::start(path.clone(), error_tx).unwrap();
        assert_eq!(logger.current_path(), Some(path.to_string_lossy().to_string()));
        logger.stop();
        let _ = std::fs::remove_file(&path);
    }
}
```

- [ ] **Step 2: 运行测试**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo test --bin neoserial logging::file_logger`
Expected: 2 passed; 0 failed

- [ ] **Step 3: 提交**

```bash
git add src/logging/file_logger.rs
git commit -m "refactor: FileLogger 改为写格式化文本行"
```

---

## Task 5: 连接层改造（DisplayConfig + 文本存盘 + 字节统计）

**Files:**
- Modify: `src/connection/handle.rs`
- Modify: `src/connection/reader.rs`
- Modify: `src/connection/mod.rs`
- Test: 主要靠现有 reader 逻辑 + 手动；本 Task 加 reader 单元测试若有纯逻辑可抽（字节统计可抽纯函数）

**Interfaces:**
- Consumes: `crate::util::log_format::{DisplayConfig, format_line}`（Task 1）、`crate::buffer::log_line::{LogLine, Dir}`
- Produces:
  - `ConnCommand::SetLogger(Option<Sender<String>>)`（类型从 Vec<u8> 改 String）
  - `ConnCommand::SetDisplay(DisplayConfig)`（新增，读线程据此过滤/格式化）
  - `ConnEvent::Stats { tx_bytes: u64, rx_bytes: u64 }`（新增）
  - `spawn_connection` 签名：`log_tx: Option<Sender<String>>`、新增 `display: DisplayConfig` 参数

**说明**：读线程持有当前 `DisplayConfig`（初始值由 spawn 传入，运行中可被 `SetDisplay` 更新）。切出完整行后：
- 生成 LogLine（已有）
- 若 `log_tx` 存在：`format_line(&line, &display, is_hex)` 非空则 send 给 FileLogger（is_hex 用 display 决定？不——存盘内容模式应由用户选择，但 MVP 存盘固定用 ASCII 文本更可读，hex dump 进日志太宽。**决定：存盘固定用 ASCII 内容**，即 `format_line(&line, &display, false)`）
- TX 行同样：发送后回显进日志流时，若 display.log_send=false 则不进 event（也不进存盘）；但**字节统计照算**
- 累计 tx_bytes/rx_bytes，每 N 字节或每行上报 `ConnEvent::Stats`

- [ ] **Step 1: 改 handle.rs**

```rust
use crossbeam_channel::Sender;
use crate::util::log_format::DisplayConfig;

/// UI → 后台线程的命令。
pub enum ConnCommand {
    /// 发送字节。
    Send(Vec<u8>),
    /// 关闭连接。
    Close,
    /// 更新存盘 sender。Some=开启存盘，None=关闭。
    SetLogger(Option<Sender<String>>),
    /// 更新显示配置（时间戳/记录发送开关），运行中可动态改。
    SetDisplay(DisplayConfig),
}

/// 后台线程 → UI 的事件。
pub enum ConnEvent {
    /// 新收到的若干完整行（已按 log_send 过滤：log_send=false 时不含 TX 行）。
    Lines(Vec<crate::buffer::log_line::LogLine>),
    /// 连接断开。
    Disconnected(String),
    /// 发送失败。
    SendFailed(String),
    /// 收发字节统计（累计值，周期上报）。
    Stats { tx_bytes: u64, rx_bytes: u64 },
}

pub struct ConnectionHandle {
    pub(crate) cmd_tx: Sender<ConnCommand>,
}

impl ConnectionHandle {
    pub fn send_command(&self, cmd: ConnCommand) {
        let _ = self.cmd_tx.send(cmd);
    }
}
```

- [ ] **Step 2: 改 writer.rs（handle_send 不变，但 SetDisplay 需在 reader 处理）**

writer.rs 的 `handle_send` match 加 `ConnCommand::SetDisplay(_) => true,`（占位，实际在 reader_loop 处理）：

```rust
        ConnCommand::SetLogger(_) => true,
        ConnCommand::SetDisplay(_) => true,
```

- [ ] **Step 3: 改 reader.rs**

完整替换 `reader_loop`：

```rust
use crossbeam_channel::{Receiver, Sender};
use serialport::SerialPort;
use std::io::Read;
use std::time::Duration;

use crate::buffer::log_line::{Dir, LogLine};
use crate::connection::handle::{ConnCommand, ConnEvent};
use crate::connection::line_assembler::LineAssembler;
use crate::connection::writer::{handle_send, try_recv_command};
use crate::util::log_format::{format_line, DisplayConfig};
use crate::util::time_fmt::now_local_ts;

/// 读线程主循环。
/// 存盘（格式化文本日志）：log_tx 存在时，切出完整行后按 DisplayConfig 生成
/// "[ts] [tag] content" 文本行 send 给 FileLogger（独立于 UI Timer，暂停照写）。
/// 显示过滤：log_send=false 时 TX 行不进 event（也不进存盘），但字节统计照算。
pub fn reader_loop(
    mut port: Box<dyn SerialPort>,
    cmd_rx: Receiver<ConnCommand>,
    event_tx: Sender<ConnEvent>,
    error_keywords: Vec<String>,
    mut log_tx: Option<Sender<String>>,
    mut display: DisplayConfig,
) {
    let _ = port.set_timeout(Duration::from_millis(10));
    let mut assembler = LineAssembler::new();
    let mut buf = [0u8; 1024];
    let mut tx_bytes: u64 = 0;
    let mut rx_bytes: u64 = 0;
    let mut stats_since_report: u64 = 0;

    loop {
        while let Some(cmd) = try_recv_command(&cmd_rx) {
            match &cmd {
                ConnCommand::SetLogger(new_tx) => {
                    log_tx = new_tx.clone();
                    continue;
                }
                ConnCommand::SetDisplay(d) => {
                    display = *d;
                    continue;
                }
                _ => {}
            }
            if !handle_send(&cmd, port.as_mut(), &event_tx) {
                return; // Close
            }
            if let ConnCommand::Send(bytes) = &cmd {
                tx_bytes += bytes.len() as u64;
                stats_since_report += bytes.len() as u64;
                // TX 行：log_send=true 才进日志流和存盘
                if display.log_send {
                    let line = LogLine::new(now_local_ts(), Dir::Tx, bytes.clone(), &error_keywords);
                    if let Some(tx) = &log_tx {
                        let s = format_line(&line, &display, false);
                        if !s.is_empty() {
                            let _ = tx.send(s);
                        }
                    }
                    let _ = event_tx.send(ConnEvent::Lines(vec![line]));
                }
            }
        }

        match port.read(&mut buf) {
            Ok(0) => {
                let _ = event_tx.send(ConnEvent::Disconnected("端口 EOF".into()));
                return;
            }
            Ok(n) => {
                rx_bytes += n as u64;
                stats_since_report += n as u64;
                let new_lines: Vec<LogLine> = assembler
                    .feed(&buf[..n])
                    .into_iter()
                    .map(|raw| LogLine::new(now_local_ts(), Dir::Rx, raw, &error_keywords))
                    .collect();
                // 存盘：每行格式化后送 FileLogger
                if let Some(tx) = &log_tx {
                    for l in &new_lines {
                        let s = format_line(l, &display, false);
                        if !s.is_empty() {
                            let _ = tx.send(s);
                        }
                    }
                }
                if !new_lines.is_empty() {
                    let _ = event_tx.send(ConnEvent::Lines(new_lines));
                }
            }
            Err(ref e) if e.kind() == std::io::ErrorKind::TimedOut => {}
            Err(ref e) if e.kind() == std::io::ErrorKind::WouldBlock => {}
            Err(e) => {
                let _ = event_tx.send(ConnEvent::Disconnected(format!("连接已断开: {}", e)));
                return;
            }
        }

        // 每 256 字节上报一次统计
        if stats_since_report >= 256 {
            stats_since_report = 0;
            let _ = event_tx.send(ConnEvent::Stats { tx_bytes, rx_bytes });
        }
    }
}
```

- [ ] **Step 4: 改 mod.rs 的 spawn_connection**

```rust
use crossbeam_channel::{bounded, Sender};

use crate::config::settings::{DataBits, FlowControl, Parity, StopBits};
use crate::connection::handle::{ConnCommand, ConnEvent, ConnectionHandle};
use crate::util::log_format::DisplayConfig;

pub mod handle;
pub mod line_assembler;
pub mod reader;
pub mod writer;

pub fn spawn_connection(
    port_name: &str,
    baud: u32,
    data_bits: DataBits,
    parity: Parity,
    stop_bits: StopBits,
    flow_control: FlowControl,
    error_keywords: Vec<String>,
    event_tx: Sender<ConnEvent>,
    log_tx: Option<Sender<String>>,
    display: DisplayConfig,
) -> std::io::Result<ConnectionHandle> {
    let port = serialport::new(port_name, baud)
        .data_bits(map_data_bits(data_bits))
        .parity(map_parity(parity))
        .stop_bits(map_stop_bits(stop_bits))
        .flow_control(map_flow_control(flow_control))
        .open()
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e))?;

    let (cmd_tx, cmd_rx) = bounded::<ConnCommand>(64);

    std::thread::Builder::new()
        .name(format!("neoserial-reader-{}", port_name))
        .spawn(move || {
            reader::reader_loop(port, cmd_rx, event_tx, error_keywords, log_tx, display);
        })?;

    Ok(ConnectionHandle { cmd_tx })
}
```
（`map_*` 函数不变）

- [ ] **Step 5: 编译验证（app.rs 还没改，会报调用不匹配，先只确认 connection 模块本身编译）**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | grep -E "error\[|error:" | head`
Expected: 错误都来自 app.rs 的 spawn_connection 调用（参数不匹配），connection 模块本身无错。

- [ ] **Step 6: 提交**

```bash
git add src/connection/
git commit -m "refactor: 连接层改文本存盘 + DisplayConfig + 字节统计"
```

---

## Task 6: theme.slint 加白底日志配色

**Files:**
- Modify: `ui/styles/theme.slint`

- [ ] **Step 1: 改 theme.slint**

```slint
export global Theme {
    out property <color> bg: #1e2227;
    out property <color> panel: #262b33;
    out property <color> panel-light: #2f353f;

    // 日志区：白底黑字
    out property <color> log-bg: #ffffff;
    out property <color> log-rx: #1f2328;      // 接收：近黑
    out property <color> log-tx: #1a7f37;      // 发送：深绿（白底可辨）
    out property <color> log-error: #cf222e;   // 错误：红
    out property <color> log-muted: #6e7681;   // 时间戳/tag：灰

    out property <color> text-rx: #c8d3e0;
    out property <color> text-tx: #7ee787;
    out property <color> text-error: #ff7b72;
    out property <color> text-muted: #7d8590;
    out property <color> accent: #58a6ff;

    // 工程风格按钮色
    out property <color> btn-primary: #2f6fd6;  // 蓝：Connect/Send/Run
    out property <color> btn-danger: #c93b3b;   // 红：Disconnect/Stop/Delete
    out property <color> btn-neutral: #3a4049;  // 灰：普通功能

    out property <length> font-mono-size: 12px;
}
```

- [ ] **Step 2: 提交**

```bash
git add ui/styles/theme.slint
git commit -m "style: theme 加白底日志配色 + 工程风格按钮色"
```

---

## Task 7: seg-button 互斥按钮组组件

**Files:**
- Create: `ui/components/seg-button.slint`

**Interfaces:**
- Produces: `export component SegButton` — 通用互斥按钮组
  - `in property <[string]> options;`
  - `in property <int> current;`
  - `callback selected(int);`

**说明**：一组横向小按钮，current 索引高亮，点哪个 selected(索引)。用于行尾选择 CR/LF/CRLF/无。

- [ ] **Step 1: 写 seg-button.slint**

```slint
import { Theme } from "../styles/theme.slint";

/// 紧凑互斥按钮组。options 为选项文本列表，current 为当前选中索引。
export component SegButton inherits Rectangle {
    in property <[string]> options;
    in property <int> current;
    callback selected(int);

    HorizontalLayout {
        spacing: 0px;
        for opt[i] in root.options : Rectangle {
            width: self.preferred-width;
            background: i == root.current ? Theme.accent : Theme.btn-neutral;
            border-width: 1px;
            border-color: Theme.bg;
            Text {
                text: opt;
                color: i == root.current ? #ffffff : #d0d7de;
                horizontal-alignment: center;
                vertical-alignment: center;
                font-size: 11px;
            }
            TouchArea {
                clicked => { root.selected(i); }
            }
        }
    }
}
```

- [ ] **Step 2: 编译验证（无独立测试，靠 build 通过）**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -3`
Expected: Finished（seg-button 未被引用，但应编译通过；若有 unused 警告无妨）

- [ ] **Step 3: 提交**

```bash
git add ui/components/seg-button.slint
git commit -m "feat: SegButton 互斥按钮组组件"
```

---

## Task 8: 顶部连接配置栏重构（加串口参数）

**Files:**
- Modify: `ui/components/connection-bar.slint`

**Interfaces:**
- 新增 in-out 属性：`data-bits<int>`、`parity<string>`、`stop-bits<string>`、`flow-control<string>`
- 新增 callback：`data-bits-changed(int)`、`parity-changed(string)`、`stop-bits-changed(string)`、`flow-control-changed(string)`
- 保留：ports/port-name/baud-rate/status-*/connected + open/close callback

**说明**：标签左置、横向紧凑。数据位用 int ComboBox（5/6/7/8），校验/停止位/流控用 string ComboBox。

- [ ] **Step 1: 改 connection-bar.slint**

```slint
import { Button, ComboBox } from "std-widgets.slint";
import { Theme } from "../styles/theme.slint";

export struct LogLine {
    ts: string,
    text: string,
    color: color,
    dir: int,
}

export struct QuickCmd {
    label: string,
    content: string,
    mode: int,
    ending: int,
}

export component ConnectionBar inherits Rectangle {
    height: 40px;
    background: Theme.panel;

    in property <string> status-text;
    in property <color> status-color;
    in property <[string]> ports;
    in property <string> port-name;
    in property <int> baud-rate;
    in property <bool> connected;

    in-out property <int> data-bits: 8;
    in-out property <string> parity: "None";
    in-out property <string> stop-bits: "One";
    in-out property <string> flow-control: "None";

    callback port-selected(string);
    callback baud-changed(int);
    callback data-bits-changed(int);
    callback parity-changed(string);
    callback stop-bits-changed(string);
    callback flow-control-changed(string);
    callback open-clicked();
    callback close-clicked();

    HorizontalLayout {
        spacing: 6px;
        padding: 6px;

        // COM
        Text { text: "COM"; color: Theme.text-muted; vertical-alignment: center; width: 30px; }
        ComboBox {
            model: root.ports;
            current-value: root.port-name;
            width: 110px;
            selected => { root.port-selected(self.current-value); }
        }
        // Baud
        Text { text: "Baud"; color: Theme.text-muted; vertical-alignment: center; width: 34px; }
        ComboBox {
            model: ["9600", "19200", "38400", "57600", "115200", "230400", "460800", "921600"];
            current-value: "\{root.baud-rate}";
            width: 90px;
            selected => { root.baud-changed(Math.round(self.current-value.to-float())); }
        }
        // 数据位
        Text { text: "数据"; color: Theme.text-muted; vertical-alignment: center; width: 30px; }
        ComboBox {
            model: ["5", "6", "7", "8"];
            current-value: "\{root.data-bits}";
            width: 60px;
            selected => { root.data-bits-changed(self.current-value.to-float()); }
        }
        // 校验
        Text { text: "校验"; color: Theme.text-muted; vertical-alignment: center; width: 30px; }
        ComboBox {
            model: ["None", "Odd", "Even"];
            current-value: root.parity;
            width: 80px;
            selected => { root.parity-changed(self.current-value); }
        }
        // 停止位
        Text { text: "停止"; color: Theme.text-muted; vertical-alignment: center; width: 30px; }
        ComboBox {
            model: ["One", "Two"];
            current-value: root.stop-bits;
            width: 70px;
            selected => { root.stop-bits-changed(self.current-value); }
        }
        // 流控
        Text { text: "流控"; color: Theme.text-muted; vertical-alignment: center; width: 30px; }
        ComboBox {
            model: ["None", "Software", "Hardware"];
            current-value: root.flow-control;
            width: 100px;
            selected => { root.flow-control-changed(self.current-value); }
        }

        Button { text: "Connect"; clicked => { root.open-clicked(); } enabled: !root.connected; primary: true; }
        Button { text: "Disconnect"; clicked => { root.close-clicked(); } enabled: root.connected; }

        Text {
            text: root.status-text;
            color: root.status-color;
            vertical-alignment: center;
            horizontal-stretch: 1;
        }
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -5`
Expected: 编译错误来自 app.slint 未传新属性（后续 Task 修），connection-bar 本身语法无误。若 `primary: true` 在 Slint 1.17 Button 不支持则去掉该属性改用 background。

- [ ] **Step 3: 提交**

```bash
git add ui/components/connection-bar.slint
git commit -m "feat: 顶部配置栏加数据位/校验/停止位/流控 + 标签左置"
```

---

## Task 9: 工具栏加显示开关

**Files:**
- Modify: `ui/components/toolbar.slint`

**Interfaces:**
- 新增 in 属性：`show-timestamp<bool>`、`log-send<bool>`
- 新增 callback：`timestamp-toggled()`、`log-send-toggled()`
- 保留：is-hex/paused/logging/log-file-name/auto-scroll + 各 callback（含 logging-reveal）

**说明**：工具栏放显示类开关 + 操作类按钮。开关用文字按钮（开=高亮）。布局可能换行，用 HorizontalLayout + 允许溢出（Slint 工程风格接受紧凑横向）。本轮保持单行，若挤不下后续调。

- [ ] **Step 1: 改 toolbar.slint**

在现有 import 后、component 内加属性与 callback，并在 HorizontalLayout 中加按钮。完整文件：

```slint
import { Button, ComboBox } from "std-widgets.slint";
import { Theme } from "../styles/theme.slint";

export component Toolbar inherits Rectangle {
    height: 32px;
    background: Theme.panel;

    in property <bool> is-hex;
    in property <bool> paused;
    in property <bool> logging;
    in property <string> log-file-name;
    in property <bool> auto-scroll;
    in property <bool> show-timestamp;
    in property <bool> log-send;

    callback mode-changed(bool);
    callback pause-toggled();
    callback clear-clicked();
    callback logging-toggled();
    callback logging-reveal();
    callback auto-scroll-toggled();
    callback timestamp-toggled();
    callback log-send-toggled();

    HorizontalLayout {
        spacing: 6px;
        padding-left: 8px;
        padding-right: 8px;
        alignment: start;

        ComboBox {
            model: ["ASCII", "HEX"];
            current-value: root.is-hex ? "HEX" : "ASCII";
            selected => { root.mode-changed(self.current-value == "HEX"); }
        }
        Button {
            text: root.show-timestamp ? "时间 ✓" : "时间";
            clicked => { root.timestamp-toggled(); }
        }
        Button {
            text: root.log-send ? "记发送 ✓" : "记发送";
            clicked => { root.log-send-toggled(); }
        }
        Button {
            text: root.paused ? "▶ 继续" : "⏸ 暂停";
            clicked => { root.pause-toggled(); }
        }
        Button { text: "🗑 清屏"; clicked => { root.clear-clicked(); } }
        if !root.logging : Button {
            text: "📁 开始存盘";
            clicked => { root.logging-toggled(); }
        }
        if root.logging : Button {
            text: "📂 所在文件夹";
            clicked => { root.logging-reveal(); }
        }
        if root.logging : Button {
            text: "⏹ 停止存盘";
            clicked => { root.logging-toggled(); }
        }
        Button {
            text: root.auto-scroll ? "↓ 自动滚动 ✓" : "↓ 自动滚动";
            clicked => { root.auto-scroll-toggled(); }
        }
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -5`
Expected: 错误来自 app.slint 未传 show-timestamp/log-send（后续 Task 修）。

- [ ] **Step 3: 提交**

```bash
git add ui/components/toolbar.slint
git commit -m "feat: 工具栏加时间戳/记录发送开关"
```

---

## Task 10: 日志区白底 + 行格式分列

**Files:**
- Modify: `ui/components/log-view.slint`

**Interfaces:**
- LogLine struct（在 connection-bar.slint 定义）加字段：`tag<string>`（"[发送]"/"[接收]"）、`show-ts<bool>`（该行是否显示时间戳列）。
- **决定**：不在 struct 加字段，而是直接把格式化好的 text 传给 UI——UI 只显示 `line.ts` / `line.tag` / `line.text` 三列，ts/tag 可能为空字符串（开关关时）。改 LogLine struct：

```slint
export struct LogLine {
    ts: string,      // 可能为空（show_timestamp=false）
    tag: string,     // "[发送]"/"[接收]"，TX 不记录时为空（整行不显示）
    text: string,
    color: color,
    dir: int,
}
```

- [ ] **Step 1: 改 log-view.slint**

```slint
import { ListView } from "std-widgets.slint";
import { Theme } from "../styles/theme.slint";
import { LogLine } from "connection-bar.slint";

export component LogView inherits Rectangle {
    background: Theme.log-bg;   // 白底

    in property <[LogLine]> log-model;
    in property <bool> paused;

    log-list := ListView {
        y: 0;
        height: 100%;
        width: 100%;

        for line[i] in root.log-model: Rectangle {
            height: 18px;
            width: 100%;
            background: transparent;
            HorizontalLayout {
                padding-left: 8px;
                spacing: 8px;
                Text {
                    text: line.ts;
                    color: Theme.log-muted;
                    font-size: Theme.font-mono-size;
                    font-family: "Consolas";
                    // ts 为空时不占宽
                    width: line.ts == "" ? 0px : self.preferred-width;
                }
                Text {
                    text: line.tag;
                    color: line.color;
                    font-size: Theme.font-mono-size;
                    font-family: "Consolas";
                    width: self.preferred-width;
                }
                Text {
                    text: line.text;
                    color: line.color;
                    font-size: Theme.font-mono-size;
                    font-family: "Consolas";
                    overflow: elide;
                    horizontal-stretch: 1;
                }
            }
        }
    }

    if root.paused: Text {
        x: 16px; y: 8px;
        text: "⏸ 已暂停（后台继续接收）";
        color: Theme.log-error;
    }
}
```

- [ ] **Step 2: 同步改 connection-bar.slint 的 LogLine struct（Task 8 已含新 struct，此处确认一致）**

确认 connection-bar.slint 中 LogLine struct 为含 tag 字段的版本（Task 8 已写）。若 Task 8 未含 tag，在此补上。

- [ ] **Step 3: 编译验证**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -5`
Expected: 错误来自 app.rs 构造 LogLine 时缺 tag 字段（后续 Task 修）。

- [ ] **Step 4: 提交**

```bash
git add ui/components/log-view.slint ui/components/connection-bar.slint
git commit -m "feat: 日志区白底 + ts/tag/text 分列"
```

---

## Task 11: 状态栏组件

**Files:**
- Create: `ui/components/status-bar.slint`

**Interfaces:**
- in 属性：`connected<bool>`、`port-name<string>`、`baud-rate<int>`、`tx-bytes<int>`、`rx-bytes<int>`
- 无 callback（纯显示）

- [ ] **Step 1: 写 status-bar.slint**

```slint
import { Theme } from "../styles/theme.slint";

export component StatusBar inherits Rectangle {
    height: 22px;
    background: Theme.panel;

    in property <bool> connected;
    in property <string> port-name;
    in property <int> baud-rate;
    in property <int> tx-bytes;
    in property <int> rx-bytes;

    HorizontalLayout {
        spacing: 16px;
        padding-left: 8px;
        padding-right: 8px;
        alignment: start;

        HorizontalLayout {
            spacing: 4px;
            Text {
                text: "●";
                color: root.connected ? Theme.text-tx : Theme.text-error;
                vertical-alignment: center;
                font-size: 11px;
            }
            Text {
                text: root.connected ? "Connected" : "Disconnected";
                color: root.connected ? Theme.text-tx : Theme.text-error;
                vertical-alignment: center;
                font-size: 11px;
            }
        }
        Text {
            text: root.port-name == "" ? "COM -" : root.port-name;
            color: Theme.text-muted;
            vertical-alignment: center;
            font-size: 11px;
        }
        Text {
            text: "\{root.baud-rate}";
            color: Theme.text-muted;
            vertical-alignment: center;
            font-size: 11px;
        }
        Text {
            text: "TX: \{root.tx-bytes} B";
            color: Theme.text-muted;
            vertical-alignment: center;
            font-size: 11px;
        }
        Text {
            text: "RX: \{root.rx-bytes} B";
            color: Theme.text-muted;
            vertical-alignment: center;
            font-size: 11px;
        }
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -3`
Expected: Finished 或仅 app.slint 相关错误。

- [ ] **Step 3: 提交**

```bash
git add ui/components/status-bar.slint
git commit -m "feat: StatusBar 状态栏组件"
```

---

## Task 12: 发送区行尾改互斥按钮组 + HEX 开关

**Files:**
- Modify: `ui/components/send-bar.slint`

**Interfaces:**
- 保留：input-text<=>、ending-index<=>、send callback、history、add-quick-cmd
- 新增：`in-out property <bool> is-hex;`（本条发送 HEX 与否，独立于全局显示模式）
- 行尾 ComboBox 换成 SegButton

**说明**：发送区不再放快捷命令（移到侧边栏）。仅：输入框 + 行尾互斥组 + HEX 开关 + 发送。

- [ ] **Step 1: 改 send-bar.slint**

```slint
import { Button, LineEdit } from "std-widgets.slint";
import { Theme } from "../styles/theme.slint";
import { SegButton } from "seg-button.slint";

export component SendBar inherits FocusScope {
    height: 56px;
    forward-focus: input;

    in property <string> error-hint;
    in-out property <string> input-text;
    in-out property <int> ending-index;  // 0=Cr 1=Lf 2=Crlf 3=None
    in-out property <bool> is-hex;

    callback send(string, int, int);  // text, mode(0/1), ending(0..3)
    callback history-prev();
    callback history-next();

    Rectangle {
        background: Theme.panel;

        VerticalLayout {
            spacing: 4px;
            padding: 6px;

            HorizontalLayout {
                spacing: 8px;
                input := LineEdit {
                    placeholder-text: "输入命令，回车发送（↑↓ 调历史）";
                    text <=> root.input-text;
                    horizontal-stretch: 1;
                    accepted => {
                        root.send(root.input-text, root.is-hex ? 1 : 0, root.ending-index);
                    }
                }
                SegButton {
                    options: ["CR", "LF", "CRLF", "无"];
                    current: root.ending-index;
                    selected(i) => { root.ending-index = i; }
                }
                Button {
                    text: root.is-hex ? "HEX ✓" : "HEX";
                    clicked => { root.is-hex = !root.is-hex; }
                }
                Button {
                    text: "发送";
                    primary: true;
                    clicked => {
                        root.send(root.input-text, root.is-hex ? 1 : 0, root.ending-index);
                    }
                }
            }

            if root.error-hint != "": Text {
                text: root.error-hint;
                color: Theme.text-error;
                font-size: 11px;
            }
        }
    }

    key-pressed(e) => {
        if e.text == Key.UpArrow {
            root.history-prev();
            accept
        } else if e.text == Key.DownArrow {
            root.history-next();
            accept
        } else {
            reject
        }
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -5`
Expected: 仅 app.slint 接线错误。

- [ ] **Step 3: 提交**

```bash
git add ui/components/send-bar.slint
git commit -m "feat: 发送区行尾改互斥按钮组 + HEX 开关，移除快捷命令"
```

---

## Task 13: 右侧命令脚本侧边栏组件

**Files:**
- Create: `ui/components/script-panel.slint`

**Interfaces:**
- in-out 属性：`groups<[CommandGroupSlint]>`、`current-group<int>`
- CommandGroupSlint struct（本组件内定义）：
  ```slint
  export struct CommandItemSlint {
      label: string,
      content: string,
      mode: int,
      ending: int,
      enabled: bool,
  }
  export struct CommandGroupSlint {
      name: string,
      items: [CommandItemSlint],
  }
  ```
- callback：`send-command(int group, int item)`（点击某条命令发送）、`add-command()`、`edit-command(int group, int item)`、`delete-command(int group, int item)`

**说明**：本轮单 Tab，但 Tab 栏按多 Tab 设计（显示 groups 的 name 列表，current-group 选中）。命令列表每行：label 按钮（点击发送）+ 编辑/删除小按钮。底部「+」加命令。

- [ ] **Step 1: 写 script-panel.slint**

```slint
import { ListView, Button } from "std-widgets.slint";
import { Theme } from "../styles/theme.slint";

export struct CommandItemSlint {
    label: string,
    content: string,
    mode: int,
    ending: int,
    enabled: bool,
}

export struct CommandGroupSlint {
    name: string,
    items: [CommandItemSlint],
}

export component ScriptPanel inherits Rectangle {
    background: Theme.panel;
    min-width: 200px;

    in-out property <[CommandGroupSlint]> groups;
    in-out property <int> current-group: 0;

    callback send-command(int, int);   // group 索引, item 索引
    callback add-command();
    callback edit-command(int, int);
    callback delete-command(int, int);

    VerticalLayout {
        spacing: 0px;

        // Tab 栏（多 Group 预留，本轮单 Tab 也渲染）
        HorizontalLayout {
            spacing: 0px;
            for g[i] in root.groups : Rectangle {
                width: 70px;
                height: 26px;
                background: i == root.current-group ? Theme.bg : Theme.panel-light;
                border-width: 1px;
                border-color: Theme.bg;
                Text {
                    text: g.name;
                    color: i == root.current-group ? #ffffff : Theme.text-muted;
                    horizontal-alignment: center;
                    vertical-alignment: center;
                    font-size: 11px;
                }
                TouchArea {
                    clicked => { root.current-group = i; }
                }
            }
            Rectangle { horizontal-stretch: 1; background: Theme.panel-light; }
        }

        // 命令列表
        ListView {
            vertical-stretch: 1;
            for g[gi] in root.groups : if gi == root.current-group : Rectangle {
                height: 100%;
                for item[ii] in g.items : Rectangle {
                    height: 28px;
                    width: 100%;
                    border-width: 0.5px;
                    border-color: Theme.bg;
                    background: Theme.panel;
                    HorizontalLayout {
                        padding-left: 6px;
                        padding-right: 4px;
                        spacing: 4px;
                        Button {
                            text: item.label;
                            horizontal-stretch: 1;
                            clicked => { root.send-command(gi, ii); }
                        }
                        Button {
                            text: "✎";
                            width: 30px;
                            clicked => { root.edit-command(gi, ii); }
                        }
                        Button {
                            text: "✕";
                            width: 30px;
                            clicked => { root.delete-command(gi, ii); }
                        }
                    }
                }
            }
        }

        // 底部新增
        Rectangle {
            height: 32px;
            background: Theme.panel-light;
            HorizontalLayout {
                padding: 4px;
                alignment: center;
                Button {
                    text: "+ 新增命令";
                    clicked => { root.add-command(); }
                }
            }
        }
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -5`
Expected: 仅 app.slint 接线错误（未引用 ScriptPanel）。

- [ ] **Step 3: 提交**

```bash
git add ui/components/script-panel.slint
git commit -m "feat: ScriptPanel 命令脚本侧边栏框架"
```

---

## Task 14: app.slint 三层布局组装

**Files:**
- Modify: `ui/app.slint`

**Interfaces:**
- 导入新组件：StatusBar、ScriptPanel
- 新属性：data-bits/parity/stop-bits/flow-control、show-timestamp、log-send、is-hex-send、tx-bytes/rx-bytes、command-groups、current-group
- 新 callback：data-bits-changed/parity-changed/stop-bits-changed/flow-control-changed、timestamp-toggled、log-send-toggled、script-send(int,int)、script-add、script-edit(int,int)、script-delete(int,int)

- [ ] **Step 1: 改 app.slint**

```slint
import { VerticalBox } from "std-widgets.slint";

import { Theme } from "styles/theme.slint";
import { ConnectionBar, LogLine, QuickCmd } from "components/connection-bar.slint";
import { Toolbar } from "components/toolbar.slint";
import { LogView } from "components/log-view.slint";
import { SendBar } from "components/send-bar.slint";
import { StatusBar } from "components/status-bar.slint";
import { ScriptPanel, CommandGroupSlint } from "components/script-panel.slint";

export { LogLine, QuickCmd, CommandGroupSlint }

export component App inherits Window {
    title: "NeoSerial";
    min-width: 900px;
    min-height: 560px;
    background: Theme.bg;

    in-out property <[string]> ports: [];
    in-out property <string> port-name;
    in-out property <int> baud-rate: 115200;
    in-out property <string> status-text: "未连接";
    in-out property <color> status-color: #ff7b72;
    in-out property <bool> connected: false;

    in-out property <int> data-bits: 8;
    in-out property <string> parity: "None";
    in-out property <string> stop-bits: "One";
    in-out property <string> flow-control: "None";

    in-out property <[LogLine]> log-model: [];
    in-out property <bool> is-hex: false;
    in-out property <bool> paused: false;
    in-out property <bool> logging: false;
    in-out property <string> log-file-name: "";
    in-out property <bool> auto-scroll: true;
    in-out property <bool> show-timestamp: true;
    in-out property <bool> log-send: true;

    in-out property <int> tx-bytes: 0;
    in-out property <int> rx-bytes: 0;

    in-out property <[CommandGroupSlint]> command-groups: [];
    in-out property <int> current-group: 0;

    in-out property <string> input-text: "";
    in-out property <int> ending-index: 2;
    in-out property <bool> is-hex-send: false;
    in-out property <string> error-hint: "";

    callback open-clicked();
    callback close-clicked();
    callback port-selected(string);
    callback baud-changed(int);
    callback data-bits-changed(int);
    callback parity-changed(string);
    callback stop-bits-changed(string);
    callback flow-control-changed(string);
    callback mode-changed(bool);
    callback pause-toggled();
    callback clear-clicked();
    callback logging-toggled();
    callback logging-reveal();
    callback auto-scroll-toggled();
    callback timestamp-toggled();
    callback log-send-toggled();
    callback send(string, int, int);
    callback history-prev();
    callback history-next();
    callback script-send(int, int);
    callback script-add();
    callback script-edit(int, int);
    callback script-delete(int, int);

    VerticalLayout {
        spacing: 0px;

        ConnectionBar {
            ports: root.ports;
            port-name: root.port-name;
            baud-rate: root.baud-rate;
            status-text: root.status-text;
            status-color: root.status-color;
            connected: root.connected;
            data-bits: root.data-bits;
            parity: root.parity;
            stop-bits: root.stop-bits;
            flow-control: root.flow-control;
            port-selected(p) => { root.port-selected(p); }
            baud-changed(b) => { root.baud-changed(b); }
            data-bits-changed(d) => { root.data-bits-changed(d); }
            parity-changed(p) => { root.parity-changed(p); }
            stop-bits-changed(s) => { root.stop-bits-changed(s); }
            flow-control-changed(f) => { root.flow-control-changed(f); }
            open-clicked => { root.open-clicked(); }
            close-clicked => { root.close-clicked(); }
        }
        Toolbar {
            is-hex: root.is-hex;
            paused: root.paused;
            logging: root.logging;
            log-file-name: root.log-file-name;
            auto-scroll: root.auto-scroll;
            show-timestamp: root.show-timestamp;
            log-send: root.log-send;
            mode-changed(h) => { root.is-hex = h; root.mode-changed(h); }
            pause-toggled => { root.paused = !root.paused; root.pause-toggled(); }
            clear-clicked => { root.clear-clicked(); }
            logging-toggled => { root.logging-toggled(); }
            logging-reveal => { root.logging-reveal(); }
            auto-scroll-toggled => { root.auto-scroll = !root.auto-scroll; root.auto-scroll-toggled(); }
            timestamp-toggled => { root.show-timestamp = !root.show-timestamp; root.timestamp-toggled(); }
            log-send-toggled => { root.log-send = !root.log-send; root.log-send-toggled(); }
        }

        // 中间左右分栏
        HorizontalLayout {
            spacing: 0px;
            vertical-stretch: 1;

            LogView {
                log-model: root.log-model;
                paused: root.paused;
                horizontal-stretch: 7;
            }
            ScriptPanel {
                groups: root.command-groups;
                current-group: root.current-group;
                horizontal-stretch: 3;
                send-command(g, i) => { root.script-send(g, i); }
                add-command => { root.script-add(); }
                edit-command(g, i) => { root.script-edit(g, i); }
                delete-command(g, i) => { root.script-delete(g, i); }
            }
        }

        SendBar {
            input-text <=> root.input-text;
            ending-index <=> root.ending-index;
            is-hex <=> root.is-hex-send;
            error-hint: root.error-hint;
            send(t, m, e) => { root.send(t, m, e); }
            history-prev => { root.history-prev(); }
            history-next => { root.history-next(); }
        }
        StatusBar {
            connected: root.connected;
            port-name: root.port-name;
            baud-rate: root.baud-rate;
            tx-bytes: root.tx-bytes;
            rx-bytes: root.rx-bytes;
        }
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -8`
Expected: Slint 编译通过；剩余错误在 app.rs（Rust 端未实现新 callback 处理）。

- [ ] **Step 3: 提交**

```bash
git add ui/app.slint
git commit -m "feat: app.slint 三层布局组装（配置/日志+侧边栏/发送/状态栏）"
```

---

## Task 15: app.rs 接线（开关 + 串口参数 + 状态栏 + 存盘）

**Files:**
- Modify: `src/app.rs`

**Interfaces:**
- Consumes: 上述所有 UI 属性/callback + Task 1-5 的 Rust 接口
- 这是最大的接线 Task。

**关键改动点**：
1. AppState 加字段：`show_timestamp: bool`、`log_send: bool`、`tx_bytes: u64`、`rx_bytes: u64`、`command_groups: Vec<CommandGroup>`、`current_group: usize`、`data_bits/parity/stop_bits/flow_control`（从 serial_defaults 拆出动态值）。
2. `DisplayConfig` 由 `show_timestamp` + `log_send` 构造，传给 spawn_connection；开关变化时 `SetDisplay`。
3. `spawn_connection` 调用更新参数（加 display）。
4. 存盘：`SetLogger` 改 `Option<Sender<String>>`；`FileLogger::line_sender()`。
5. Timer 消费 `ConnEvent::Stats` 更新 tx_bytes/rx_bytes → set_tx_bytes/rx_bytes。
6. `refresh_model` 改用 format_line 或直接构造含 tag/ts 的 LogLine UI struct——**注意**：UI LogLine struct 现在要 ts/tag/text/color/dir。读线程发的 ConnEvent::Lines 里 LogLine 已含 ts/dir/ascii/hex。UI 侧 refresh 时按 show_timestamp 决定 ts 是否填、按 dir 填 tag。
7. TX 不记录时：读线程已过滤（不进 event），UI 无需额外处理。
8. 串口参数 callback：data-bits-changed 等更新 AppState + `SetDisplay` 不需要（串口参数改要重连才生效，MVP 记下、下次打开生效）。
9. 脚本 callback：script-send 调用 send 逻辑；script-add/edit/delete 改 command_groups + 同步 UI model + 存配置。

- [ ] **Step 1: 改 AppState 与初始化**

在 app.rs 顶部 use 区加：
```rust
use crate::config::command_group::{CommandGroup, CommandItem};
use crate::util::log_format::DisplayConfig;
```

AppState 加字段：
```rust
struct AppState {
    handle: Option<ConnectionHandle>,
    ring: RingBuffer<LogLine>,
    file_logger: Option<FileLogger>,
    log_error_rx: Option<Receiver<String>>,
    error_keywords: Vec<String>,
    serial_defaults: crate::config::settings::SerialDefaults,
    is_hex: bool,
    paused: bool,
    auto_scroll: bool,
    show_timestamp: bool,
    log_send: bool,
    tx_bytes: u64,
    rx_bytes: u64,
    command_groups: Vec<CommandGroup>,
    current_group: usize,
    send_history: Vec<String>,
}
```

初始化处（`run()` 中构造 state）补：
```rust
    show_timestamp: settings.ui.show_timestamp,
    log_send: settings.ui.log_send,
    tx_bytes: 0,
    rx_bytes: 0,
    command_groups: settings.command_groups.clone(),
    current_group: 0,
```
并 set 初始 UI：`app.set_show_timestamp(settings.ui.show_timestamp);` `app.set_log_send(settings.ui.log_send);` `app.set_data_bits(8)`（按 serial_defaults）等。把 command_groups 转 CommandGroupSlint model 设给 UI。

- [ ] **Step 2: DisplayConfig 辅助函数 + spawn_connection 改造**

加辅助：
```rust
fn display_config(st: &AppState) -> DisplayConfig {
    DisplayConfig { show_timestamp: st.show_timestamp, log_send: st.log_send }
}
```

`on_open_clicked` 中：
```rust
            let kws = st.error_keywords.clone();
            let sd = st.serial_defaults.clone();
            let log_tx = st.file_logger.as_ref().map(|fl| fl.line_sender());
            let display = display_config(&st);
            match spawn_connection(
                &port_name, baud, sd.data_bits, sd.parity, sd.stop_bits, sd.flow_control,
                kws, event_tx.clone(), log_tx, display,
            ) { ... }
```

- [ ] **Step 3: 存盘开关改文本行**

`on_logging_toggled` 中 `FileLogger::start` 后 `fl.line_sender()`，`SetLogger(Some(fl.line_sender()))`。默认文件名 `.log`：
```rust
                let default_name = format!("neoserial_{}.log", now);
```

- [ ] **Step 4: 开关 callback**

```rust
    // 时间戳开关
    {
        let weak = weak.clone();
        let state = state.clone();
        app.on_timestamp_toggled(move || {
            let app = weak.unwrap();
            let mut st = state.lock().unwrap();
            st.show_timestamp = app.get_show_timestamp();
            if let Some(h) = &st.handle {
                h.send_command(ConnCommand::SetDisplay(display_config(&st)));
            }
            refresh_model(&app, &st);
        });
    }
    // 记录发送开关
    {
        let weak = weak.clone();
        let state = state.clone();
        app.on_log_send_toggled(move || {
            let app = weak.unwrap();
            let mut st = state.lock().unwrap();
            st.log_send = app.get_log_send();
            if let Some(h) = &st.handle {
                h.send_command(ConnCommand::SetDisplay(display_config(&st)));
            }
        });
    }
```

- [ ] **Step 5: 串口参数 callback（记下，下次打开生效）**

```rust
    {
        let weak = weak.clone();
        let state = state.clone();
        app.on_data_bits_changed(move |d| {
            let app = weak.unwrap();
            let mut st = state.lock().unwrap();
            st.serial_defaults.data_bits = match d { 5=>DataBits::Five,6=>DataBits::Six,7=>DataBits::Seven,_=>DataBits::Eight };
            app.set_data_bits(d);
        });
    }
    // parity / stop_bits / flow_control 类似（string → 枚举）
```
（parity/stop/flow 需 match string；注意 import DataBits/Parity/StopBits/FlowControl）

- [ ] **Step 6: Timer 消费 Stats**

Timer 闭包 event 循环加：
```rust
                    ConnEvent::Stats { tx_bytes, rx_bytes } => {
                        stats_tx = Some(tx_bytes);
                        stats_rx = Some(rx_bytes);
                    }
```
（声明 `let mut stats_tx: Option<u64> = None; let mut stats_rx: Option<u64> = None;`）
循环后：
```rust
            if let Some(t) = stats_tx { st.tx_bytes = t; app.set_tx_bytes(t as i32); }
            if let Some(r) = stats_rx { st.rx_bytes = r; app.set_rx_bytes(r as i32); }
```

- [ ] **Step 7: refresh_model 改造（含 tag/ts）**

```rust
fn refresh_model(app: &App, st: &AppState) {
    let snapshot = st.ring.snapshot();
    let view: Vec<crate::LogLine> = snapshot
        .iter()
        .map(|l| {
            let text = if st.is_hex { &l.hex } else { &l.ascii };
            let color = if l.is_error {
                slint::Color::from_rgb_u8(0xcf, 0x22, 0x2e)
            } else {
                match l.dir {
                    Dir::Tx => slint::Color::from_rgb_u8(0x1a, 0x7f, 0x37),
                    Dir::Rx => slint::Color::from_rgb_u8(0x1f, 0x23, 0x28),
                }
            };
            let ts = if st.show_timestamp { l.ts.clone() } else { String::new() };
            let tag = if matches!(l.dir, Dir::Tx) { "[发送]".to_string() } else { "[接收]".to_string() };
            crate::LogLine {
                ts: ts.into(),
                tag: tag.into(),
                text: text.clone().into(),
                color: color.into(),
                dir: if matches!(l.dir, Dir::Tx) { 1 } else { 0 },
            }
        })
        .collect();
    let m = app.get_log_model();
    if let Some(vm) = m.as_any().downcast_ref::<VecModel<crate::LogLine>>() {
        vm.set_vec(view);
    } else {
        let new_model = VecModel::from(view);
        app.set_log_model(ModelRc::new(new_model));
    }
}
```

- [ ] **Step 8: 脚本 callback**

`on_script_send`：
```rust
    {
        let weak = weak.clone();
        let state = state.clone();
        app.on_script_send(move |g, i| {
            let app = weak.unwrap();
            let st = state.lock().unwrap();
            if let Some(grp) = st.command_groups.get(g as usize) {
                if let Some(item) = grp.items.get(i as usize) {
                    // 复用发送逻辑：构造 bytes 后 send_command
                    let bytes_result = if matches!(item.mode, DisplayMode::Hex) {
                        hex_to_bytes(&item.content)
                    } else {
                        Ok(ascii_to_bytes(&item.content))
                    };
                    if let Ok(mut bytes) = bytes_result {
                        let le = item.line_ending;
                        le.append_bytes(&mut bytes);
                        if let Some(h) = &st.handle {
                            h.send_command(ConnCommand::Send(bytes));
                        } else {
                            drop(st);
                            app.set_error_hint("未连接".into());
                        }
                    }
                }
            }
        });
    }
```

`on_script_add` / `on_script_edit` / `on_script_delete`：改 `st.command_groups`，然后重建 UI model 并写配置。抽一个辅助：
```rust
fn sync_script_model(app: &App, st: &AppState) {
    let groups: Vec<crate::CommandGroupSlint> = st.command_groups.iter().map(|g| {
        crate::CommandGroupSlint {
            name: g.name.clone().into(),
            items: ModelRc::new(VecModel::from(g.items.iter().map(|it| crate::CommandItemSlint {
                label: it.label.clone().into(),
                content: it.content.clone().into(),
                mode: if matches!(it.mode, DisplayMode::Hex) {1} else {0},
                ending: match it.line_ending { LineEnding::Cr=>0,LineEnding::Lf=>1,LineEnding::Crlf=>2,LineEnding::None=>3 },
                enabled: it.enabled,
            }).collect::<Vec<_>>())),
        }
    }).collect();
    app.set_command_groups(ModelRc::new(VecModel::from(groups)));
}
```
add：push 一个空 CommandItem（label="新命令"），edit：MVP 用简单方式——把该命令内容填入发送输入框供用户改后重发（完整编辑面板留 v2），delete：remove。

> **编辑简化说明**：完整编辑面板（弹窗/行内编辑）留 v2。本轮 `edit-command` 的行为：把该命令的 content 填入底部输入框，用户可改后发送或手动重存。`add-command`：push 一个占位项并立即同步。这满足「框架搭好 + 可增删」，编辑降级为「载入到输入框」。

- [ ] **Step 9: on_close_requested 持久化扩展**

关闭时保存加：
```rust
            settings.ui.show_timestamp = st.show_timestamp;
            settings.ui.log_send = st.log_send;
            settings.command_groups = st.command_groups.clone();
            settings.serial_defaults.data_bits = st.serial_defaults.data_bits;
            settings.serial_defaults.parity = st.serial_defaults.parity;
            settings.serial_defaults.stop_bits = st.serial_defaults.stop_bits;
            settings.serial_defaults.flow_control = st.serial_defaults.flow_control;
```

- [ ] **Step 10: 删除/清理旧的 quick_commands 相关接线**

app.rs 中原 `on_add_quick_cmd`、`qc_model` 构造、`set_quick_commands` 等移除或改为 command_groups。原 on_send 的 `fl.log` 已无（Task 4 删了 log 方法），确认无残留引用。

- [ ] **Step 11: 编译并修复**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build 2>&1 | tail -20`
逐个修编译错误（类型不匹配、缺 import、unused）。目标：build 通过。

- [ ] **Step 12: 跑涉及变更的测试**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo test --bin neoserial 2>&1 | tail -10`
Expected: 全绿（纯逻辑模块未受影响，app.rs 无单元测试）。

- [ ] **Step 13: 提交**

```bash
git add src/app.rs
git commit -m "feat: app.rs 接线开关/串口参数/状态栏/侧边栏/文本存盘"
```

---

## Task 16: 端到端编译 + 手动验证清单更新

**Files:**
- Modify: `README.md`

- [ ] **Step 1: release 构建**

Run: `export PATH="/s/rust/.cargo/bin:$PATH" && cargo build --release 2>&1 | tail -5`
Expected: Finished release。

- [ ] **Step 2: 更新 README 验证清单 + 架构描述**

更新 README：
- 使用说明改为三层布局描述。
- 手动验证清单更新（加：时间戳开关、记录发送开关、侧边栏增删改、状态栏字节计数、串口参数切换）。
- 架构描述：存盘改为格式化文本日志（读线程按 DisplayConfig 生成行文本直送 FileLogger）。

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -m "docs: README 更新三层布局 + 新验证清单"
git push
```

---

## Self-Review 检查

**1. Spec 覆盖**：
- §2 三层布局 → Task 14 ✓
- §3 顶部配置栏（含数据位/校验/停止/流控）→ Task 8 + Task 15 Step 5 ✓
- §4 日志区白底 → Task 6(theme) + Task 10 ✓
- §5 侧边栏框架 → Task 13 + Task 15 Step 8 ✓
- §6 发送区互斥行尾 + 状态栏 → Task 7(seg) + Task 12 + Task 11 + Task 15 Step 6 ✓
- §7 显示开关 → Task 9 + Task 15 Step 4 ✓
- §8 格式化存盘 → Task 1(format) + Task 4(file_logger) + Task 5(reader) + Task 15 Step 3 ✓
- §9 配置持久化 → Task 3 + Task 15 Step 9 ✓
- §10 架构影响 → Task 5 ✓

**2. 类型一致性**：
- `SetLogger(Option<Sender<String>>)` — Task 4/5/15 一致 ✓
- `DisplayConfig { show_timestamp, log_send }` — Task 1/5/15 一致 ✓
- `ConnEvent::Stats { tx_bytes, rx_bytes }` — Task 5/15 一致 ✓
- UI LogLine struct 含 tag — Task 8/10/14/15 一致 ✓
- `CommandGroupSlint`/`CommandItemSlint` — Task 13/14/15 一致 ✓

**3. 已知简化（spec 内说明）**：
- 侧边栏编辑降级为「载入到输入框」（Task 15 Step 8），完整编辑面板留 v2。
- 单 Tab（多 Tab 模型预留）。
- 存盘内容固定 ASCII（hex dump 不进日志）。
